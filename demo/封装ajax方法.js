$.ajax({
  suc: function () {

  }
})

// function ajax(url, callback) {
//   // var callback = handler
//   var xhr = new XMLHttpRequest()
//   xhr.open(url)
//   xhr.onreadystatechange = function () {
//     if (true) {
//       // 调用 callback 就是在调用 handler
//       callback(xhr.responseText)
//     }
//   }
// }

// var handler = function (data) {
  
// }

// // 函数能当作参数进行传递
// $.get('地址', handler)
